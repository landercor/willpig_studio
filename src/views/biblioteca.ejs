<!DOCTYPE html>
<html lang="es">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Biblioteca | Willpig Studio</title>
  <link rel="stylesheet" href="/css/style1.2.css" />
  <link rel="icon" href="/img/img.ico/logo.ico">
</head>

<body>
  </header>

  <!-- ===== BIBLIOTECA ===== -->
  <section class="library-page">
    <div class="library-header">
      <h1>Biblioteca</h1>
      <div class="library-tabs">
        <button class="tab-btn active" data-tab="current-reads">Lecturas actuales</button>
        <button class="tab-btn" data-tab="reading-lists">Listas de lectura</button>
      </div>
    </div>

    <div class="library-content">
      <!-- === Lecturas actuales === -->
      <div id="current-reads" class="tab-content active">
        <div class="section-title">Todas las historias</div>

        <div class="library-grid">

          <% libros.forEach(libro=> { %>
            <div class="library-item">

              <a href="/historias/<%= libro.id_cuento || '#' %>">
                <img src="<%= libro.url_portada || '/img/default-portada.jpg' %>" class="cover"
                  alt="<%= libro.titulo %>">
              </a>

              <p class="title">
                <%= libro.titulo %>
              </p>

              <div class="author">
                <img src="/img/img.ico/user-icon.png" alt="autor">
                <span>@<%= (libro.cuenta_usuario && libro.cuenta_usuario.username) || 'autor' %></span>
              </div>

              <div class="meta">
                <img src="/img/img.ico/eye.png" alt="vistas" class="icon-eye">
                <%= libro.cantidad_me_gusta || 0 %>
              </div>

            </div>
            <% }) %>

        </div>


        <!-- === Listas de lectura === -->
        <div id="reading-lists" class="tab-content">
          <div class="section-title">Tus listas de lectura</div>
          <p>Aquí aparecerán tus listas personalizadas.</p>
        </div>
      </div>
  </section>
  <%- include('partials/footer') %>

    <script>
      // ==== Tabs funcionales ====
      const tabs = document.querySelectorAll('.tab-btn');
      const contents = document.querySelectorAll('.tab-content');

      tabs.forEach(tab => {
        tab.addEventListener('click', () => {
          tabs.forEach(btn => btn.classList.remove('active'));
          contents.forEach(content => content.classList.remove('active'));

          tab.classList.add('active');
          document.getElementById(tab.dataset.tab).classList.add('active');
        });
      });
    </script>

</body>

</html>